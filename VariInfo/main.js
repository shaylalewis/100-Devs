let name = "John";
let admin = name;

alert(admin);

let homePlanet = "Earth";
let currentVisitor = userName;

function checkAge(age) {
    return (age > 18) ? true : confirm('Did parents allow you?');
}
function checkAge(age) {
    return (age > 18) ? true : confirm('Did')
}